---
- name: Verify
  hosts: all
  tasks:
    - name: Ensure ld.so.preload is slurped
      tags:
        - snoopy
      ansible.builtin.slurp:
        src: /etc/ld.so.preload
      register: __snoopy_ld_so_preload

    - name: Ensure ld.so.preload is verified
      tags:
        - snoopy
      ansible.builtin.assert:
        that:
          - "'snoopy' in __snoopy_ld_so_preload['content'] | b64decode"
...
