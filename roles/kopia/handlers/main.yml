---
- name: Restart kopia
  tags:
    - kopia
  become: true
  ansible.builtin.systemd_service:
    name: kopia.service
    state: restarted
  when:
    - kopia_repository | length > 0
    - kopia_repository.state | d('present') == 'present'
...
