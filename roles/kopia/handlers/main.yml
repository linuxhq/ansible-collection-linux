---
- name: Restart kopia
  tags:
    - kopia
  become: true
  ansible.builtin.systemd_service:
    name: kopia.service
    state: restarted
  when:
    - __kopia_unit.stat.exists | d(false) or not ansible_check_mode
...
