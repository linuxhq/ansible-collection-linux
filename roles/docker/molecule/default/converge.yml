---
- name: Converge
  hosts: all
  roles:
    - docker

    - role: docker
      docker_daemon:
        experimental: false
        ipv6: false
        ip6tables: false
        live-restore: true
      docker_users:
        - vagrant
...
